'use client'
import { useCallback, useEffect, useState } from 'react'
import { ClipboardList, Plus, CheckCircle2, Package, XCircle, Loader2, Trash2 } from 'lucide-react'
import { Card, CardContent } from '@/components/ui/card'
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select'
import { Dialog, DialogContent, DialogFooter, DialogHeader, DialogTitle } from '@/components/ui/dialog'
import { Badge } from '@/components/ui/badge'
import { Button } from '@/components/ui/button'
import { Input } from '@/components/ui/input'
import { Label } from '@/components/ui/label'
import { Skeleton } from '@/components/ui/skeleton'
import { useToast } from '@/hooks/use-toast'
import { formatCurrency, formatDate } from '@/lib/utils'

interface PO { id: string; poNumber: string; status: string; totalAmount: number; currency: string; notes: string; createdAt: string; supplier: { name: string } | null; shop: { name: string } | null; lineItems: string }
const STATUS_COLORS: Record<string, string> = { draft: 'bg-gray-100 text-gray-800', approved: 'bg-blue-100 text-blue-800', received: 'bg-green-100 text-green-800', cancelled: 'bg-red-100 text-red-800' }

export function PurchaseOrdersPage() {
  const { toast } = useToast()
  const [pos, setPos] = useState<PO[]>([])
  const [loading, setLoading] = useState(true)
  const [statusFilter, setStatusFilter] = useState('all')
  const [dialogOpen, setDialogOpen] = useState(false)
  const [saving, setSaving] = useState(false)
  const [formShopId, setFormShopId] = useState(''); const [formSupplierId, setFormSupplierId] = useState(''); const [formNotes, setFormNotes] = useState('')
  const [lineItems, setLineItems] = useState<Array<{ partId: string; quantity: number; unitCost: number; totalCost: number; batchNumber?: string; expiryDate?: string }>>([{ partId: '', quantity: 1, unitCost: 0, totalCost: 0 }])
  const [parts, setParts] = useState<Array<{ id: string; partNumber: string; name: string; costPrice: number }>>([])
  const [shops, setShops] = useState<Array<{ id: string; name: string }>>([])
  const [suppliers, setSuppliers] = useState<Array<{ id: string; name: string }>>([])

  const fetchPOs = useCallback(async () => { setLoading(true); try { const url = `/api/purchase-orders${statusFilter !== 'all' ? `?status=${statusFilter}` : ''}`; const res = await fetch(url); if (!res.ok) throw new Error('Failed'); setPos((await res.json()).purchaseOrders) } catch (err) { toast({ title: 'Error', description: err instanceof Error ? err.message : 'Failed', variant: 'destructive' }) } finally { setLoading(false) } }, [statusFilter, toast])
  useEffect(() => { void fetchPOs() }, [fetchPOs])

  const openCreate = async () => { setFormShopId(''); setFormSupplierId(''); setFormNotes(''); setLineItems([{ partId: '', quantity: 1, unitCost: 0, totalCost: 0 }]); setDialogOpen(true); try { const [p, s, sup] = await Promise.all([fetch('/api/parts?limit=200').then(r => r.json()), fetch('/api/shops').then(r => r.json()), fetch('/api/suppliers').then(r => r.json())]); setParts(p.parts || []); setShops(s.shops || []); setSuppliers(sup.suppliers || sup || []) } catch {} }
  const updateLine = (i: number, field: string, value: string | number) => { const u = [...lineItems]; if (field === 'partId') { const p = parts.find(x => x.id === value); u[i] = { ...u[i], partId: value as string, unitCost: p?.costPrice ?? 0 }; u[i].totalCost = u[i].quantity * u[i].unitCost } else if (field === 'quantity' || field === 'unitCost') { u[i] = { ...u[i], [field]: Number(value) }; u[i].totalCost = u[i].quantity * u[i].unitCost } else { u[i] = { ...u[i], [field]: value } }; setLineItems(u) }
  const total = lineItems.reduce((s, l) => s + l.totalCost, 0)

  const handleCreate = async () => { if (lineItems.some(l => !l.partId || l.quantity <= 0)) { toast({ title: 'Add valid line items', variant: 'destructive' }); return } setSaving(true); try { const res = await fetch('/api/purchase-orders', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ shopId: formShopId || undefined, supplierId: formSupplierId || undefined, notes: formNotes, lineItems }) }); if (!res.ok) throw new Error((await res.json()).error || 'Failed'); toast({ title: 'PO created' }); setDialogOpen(false); void fetchPOs() } catch (err) { toast({ title: 'Error', description: err instanceof Error ? err.message : 'Failed', variant: 'destructive' }) } finally { setSaving(false) } }
  const handleAction = async (id: string, action: string) => { setSaving(true); try { const res = await fetch(`/api/purchase-orders/${id}`, { method: 'PATCH', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ action }) }); if (!res.ok) throw new Error((await res.json()).error || 'Failed'); toast({ title: `PO ${action}d` }); void fetchPOs() } catch (err) { toast({ title: 'Error', description: err instanceof Error ? err.message : 'Failed', variant: 'destructive' }) } finally { setSaving(false) } }

  if (loading) return (<div className="space-y-4 p-6"><Skeleton className="h-8 w-64" /><Skeleton className="h-96" /></div>)
  return (
    <div className="space-y-6 p-6">
      <div className="flex items-center justify-between"><div><h2 className="text-2xl font-bold flex items-center gap-2"><ClipboardList className="h-6 w-6 text-indigo-600" />Purchase Orders</h2><p className="text-sm text-gray-600 mt-1">Workflow: draft → approved → received. Receiving auto-increments stock.</p></div><Button onClick={openCreate}><Plus className="h-4 w-4 mr-2" />New PO</Button></div>
      <div className="flex items-center gap-2"><span className="text-sm">Filter:</span><Select value={statusFilter} onValueChange={setStatusFilter}><SelectTrigger className="w-40"><SelectValue /></SelectTrigger><SelectContent><SelectItem value="all">All</SelectItem><SelectItem value="draft">Draft</SelectItem><SelectItem value="approved">Approved</SelectItem><SelectItem value="received">Received</SelectItem><SelectItem value="cancelled">Cancelled</SelectItem></SelectContent></Select></div>
      <Card><CardContent className="pt-6">{pos.length === 0 ? <div className="text-center py-12 text-gray-500"><ClipboardList className="h-12 w-12 mx-auto mb-2 opacity-30" /><p>No purchase orders yet.</p></div> : <div className="overflow-x-auto"><table className="w-full text-sm"><thead><tr className="border-b text-left bg-gray-50"><th className="py-3 px-2">PO #</th><th className="py-3 px-2">Supplier</th><th className="py-3 px-2">Shop</th><th className="py-3 px-2">Status</th><th className="py-3 px-2 text-right">Total</th><th className="py-3 px-2">Created</th><th className="py-3 px-2">Actions</th></tr></thead><tbody>{pos.map((po) => (<tr key={po.id} className="border-b hover:bg-gray-50"><td className="py-3 px-2 font-mono font-semibold">{po.poNumber}</td><td className="py-3 px-2">{po.supplier?.name || '—'}</td><td className="py-3 px-2">{po.shop?.name || '—'}</td><td className="py-3 px-2"><Badge className={STATUS_COLORS[po.status]}>{po.status}</Badge></td><td className="py-3 px-2 text-right font-semibold">{formatCurrency(po.totalAmount, po.currency)}</td><td className="py-3 px-2 text-xs text-gray-500">{formatDate(po.createdAt)}</td><td className="py-3 px-2"><div className="flex gap-1">{po.status === 'draft' && <Button size="sm" variant="outline" onClick={() => handleAction(po.id, 'approve')}><CheckCircle2 className="h-3 w-3 mr-1" />Approve</Button>}{po.status === 'approved' && <Button size="sm" onClick={() => handleAction(po.id, 'receive')}><Package className="h-3 w-3 mr-1" />Receive</Button>}{(po.status === 'draft' || po.status === 'approved') && <Button size="sm" variant="outline" onClick={() => handleAction(po.id, 'cancel')}><XCircle className="h-3 w-3 mr-1" />Cancel</Button>}</div></td></tr>))}</tbody></table></div>}</CardContent></Card>
      <Dialog open={dialogOpen} onOpenChange={setDialogOpen}><DialogContent className="max-w-3xl max-h-[90vh] overflow-y-auto"><DialogHeader><DialogTitle>New Purchase Order</DialogTitle></DialogHeader><div className="space-y-4"><div className="grid grid-cols-2 gap-3"><div><Label>Shop</Label><Select value={formShopId} onValueChange={setFormShopId}><SelectTrigger><SelectValue placeholder="Optional" /></SelectTrigger><SelectContent>{shops.map((s) => <SelectItem key={s.id} value={s.id}>{s.name}</SelectItem>)}</SelectContent></Select></div><div><Label>Supplier</Label><Select value={formSupplierId} onValueChange={setFormSupplierId}><SelectTrigger><SelectValue placeholder="Optional" /></SelectTrigger><SelectContent>{suppliers.map((s) => <SelectItem key={s.id} value={s.id}>{s.name}</SelectItem>)}</SelectContent></Select></div></div><div><Label>Line Items</Label><div className="space-y-2 mt-2">{lineItems.map((line, i) => (<div key={i} className="grid grid-cols-12 gap-2 items-end p-2 border rounded"><div className="col-span-4"><Label className="text-xs">Part</Label><Select value={line.partId} onValueChange={(v) => updateLine(i, 'partId', v)}><SelectTrigger className="h-8"><SelectValue placeholder="Select" /></SelectTrigger><SelectContent>{parts.map((p) => <SelectItem key={p.id} value={p.id}>{p.partNumber} — {p.name}</SelectItem>)}</SelectContent></Select></div><div className="col-span-1"><Label className="text-xs">Qty</Label><Input type="number" min={1} value={line.quantity} onChange={(e) => updateLine(i, 'quantity', e.target.value)} className="h-8" /></div><div className="col-span-2"><Label className="text-xs">Unit Cost</Label><Input type="number" min={0} step="0.01" value={line.unitCost} onChange={(e) => updateLine(i, 'unitCost', e.target.value)} className="h-8" /></div><div className="col-span-2"><Label className="text-xs">Batch #</Label><Input value={line.batchNumber || ''} onChange={(e) => updateLine(i, 'batchNumber', e.target.value)} className="h-8" /></div><div className="col-span-2"><Label className="text-xs">Expiry</Label><Input type="date" value={line.expiryDate || ''} onChange={(e) => updateLine(i, 'expiryDate', e.target.value)} className="h-8" /></div><div className="col-span-1"><Button size="icon" variant="ghost" onClick={() => setLineItems(lineItems.filter((_, idx) => idx !== i))} disabled={lineItems.length === 1}><Trash2 className="h-4 w-4 text-red-500" /></Button></div></div>))}</div><Button variant="outline" size="sm" onClick={() => setLineItems([...lineItems, { partId: '', quantity: 1, unitCost: 0, totalCost: 0 }])} className="mt-2"><Plus className="h-3 w-3 mr-1" />Add Line</Button></div><div><Label>Notes</Label><Input value={formNotes} onChange={(e) => setFormNotes(e.target.value)} /></div><div className="text-right font-semibold">Total: {formatCurrency(total)}</div></div><DialogFooter><Button variant="outline" onClick={() => setDialogOpen(false)}>Cancel</Button><Button onClick={handleCreate} disabled={saving}>{saving && <Loader2 className="h-4 w-4 mr-2 animate-spin" />}Create Draft PO</Button></DialogFooter></DialogContent></Dialog>
    </div>
  )
}
